# Trading Dashboard Frontend

React/TypeScript client for the real-time trading dashboard.

## Commands

```bash
npm install
npm run dev
npm run build
npm run lint
```

The Vite dev server proxies `/api` and `/ws` to the FastAPI backend on `127.0.0.1:8000`.
